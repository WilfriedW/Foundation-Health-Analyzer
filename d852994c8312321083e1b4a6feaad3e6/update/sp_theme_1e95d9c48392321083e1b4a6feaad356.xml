<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_theme">
    <sp_theme action="INSERT_OR_UPDATE">
        <css_variables>/* ============================================
   FOUNDATION HEALTH ANALYZER - UNIFIED THEME
   ============================================ */

/* CSS Variables */
:root {
  --fha-primary: #0378BE;
  --fha-primary-dark: #035B8E;
  --fha-primary-light: #E3F2FD;
  --fha-success: #28A745;
  --fha-warning: #FFC107;
  --fha-danger: #DC3545;
  --fha-info: #17A2B8;
  --fha-gray-100: #F8F9FA;
  --fha-gray-200: #E9ECEF;
  --fha-gray-500: #6C757D;
  --fha-gray-800: #343A40;
  --fha-header-bg: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  --fha-header-accent: #00d4ff;
  --fha-radius: 8px;
  --fha-radius-lg: 10px;
  --fha-shadow: 0 2px 10px rgba(0, 0, 0, 0.08);
  --fha-shadow-lg: 0 4px 20px rgba(0, 0, 0, 0.15);
  --fha-transition: all 0.2s ease;
}

/* ============================================
   HEADER STYLES
   ============================================ */
.fha-header {
  background: var(--fha-header-bg);
  padding: 0;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
  position: sticky;
  top: 0;
  z-index: 1000;
  animation: fha-fadeIn 0.3s ease;
}

.fha-header-container {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24px;
  height: 64px;
}

.fha-logo {
  display: flex;
  align-items: center;
  gap: 12px;
  cursor: pointer;
  transition: opacity 0.2s ease;
}

.fha-logo:hover { opacity: 0.9; }

.fha-logo-icon {
  width: 40px;
  height: 40px;
  background: linear-gradient(135deg, var(--fha-header-accent) 0%, #0099cc 100%);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  color: white;
  box-shadow: 0 4px 12px rgba(0, 212, 255, 0.3);
}

.fha-logo-text { display: flex; flex-direction: column; }
.fha-logo-title { font-size: 18px; font-weight: 700; color: #ffffff; letter-spacing: 0.5px; line-height: 1.2; }
.fha-logo-subtitle { font-size: 11px; color: rgba(255, 255, 255, 0.6); text-transform: uppercase; letter-spacing: 1px; }

.fha-nav { display: flex; align-items: center; gap: 8px; }

.fha-nav-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  color: rgba(255, 255, 255, 0.7);
  text-decoration: none;
  font-size: 14px;
  font-weight: 500;
  border-radius: 8px;
  transition: var(--fha-transition);
  cursor: pointer;
  border: none;
  background: transparent;
}

.fha-nav-item:hover { color: #ffffff; background: rgba(255, 255, 255, 0.1); }
.fha-nav-item.active { color: var(--fha-header-accent); background: rgba(0, 212, 255, 0.15); }
.fha-nav-item i { font-size: 16px; }

.fha-user-section { display: flex; align-items: center; gap: 16px; }
.fha-user-info { display: flex; align-items: center; gap: 10px; padding: 6px 12px; background: rgba(255, 255, 255, 0.05); border-radius: 20px; }
.fha-user-avatar { width: 32px; height: 32px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-size: 14px; font-weight: 600; }
.fha-user-name { color: rgba(255, 255, 255, 0.9); font-size: 13px; font-weight: 500; }

.fha-mobile-toggle { display: none; background: transparent; border: none; color: white; font-size: 24px; cursor: pointer; padding: 8px; }

/* ============================================
   PAGE LAYOUT
   ============================================ */
.fha-dashboard, .fha-results-page {
  padding: 20px;
  max-width: 1400px;
  margin: 0 auto;
}

.fha-page-header {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  padding: 30px;
  border-radius: var(--fha-radius-lg);
  margin-bottom: 25px;
}

.fha-page-header h1, .fha-page-header h2 { margin: 0 0 10px 0; font-weight: 600; }
.fha-page-header p { margin: 0; opacity: 0.9; }

/* ============================================
   CARDS
   ============================================ */
.fha-card {
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  margin-bottom: 20px;
  overflow: hidden;
  transition: var(--fha-transition);
}

.fha-card:hover { box-shadow: var(--fha-shadow-lg); }

.fha-card-header {
  background: var(--fha-gray-100);
  padding: 15px 20px;
  border-bottom: 1px solid var(--fha-gray-200);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.fha-card-header h4 { margin: 0; font-size: 16px; font-weight: 600; color: var(--fha-gray-800); }
.fha-card-body { padding: 20px; }

/* ============================================
   STATS
   ============================================ */
.fha-stats-row, .fha-stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 20px;
  margin-bottom: 20px;
}

.fha-stat-box, .fha-stat-card {
  background: var(--fha-gray-100);
  padding: 20px;
  border-radius: var(--fha-radius);
  text-align: center;
}

.fha-stat-card { background: white; box-shadow: var(--fha-shadow); }
.fha-stat-card.highlight { border: 2px solid var(--fha-primary); }

.fha-stat-value { font-size: 28px; font-weight: 700; color: var(--fha-primary); }
.fha-stat-value.good, .fha-stat-value.success { color: var(--fha-success); }
.fha-stat-value.medium, .fha-stat-value.warning { color: var(--fha-warning); }
.fha-stat-value.poor, .fha-stat-value.danger { color: var(--fha-danger); }
.fha-stat-label { font-size: 13px; color: var(--fha-gray-500); margin-top: 5px; }

/* Health Score */
.fha-health-circle {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 15px;
  font-size: 32px;
  font-weight: 700;
}

.fha-health-circle.good { background: rgba(40, 167, 69, 0.15); color: var(--fha-success); border: 4px solid var(--fha-success); }
.fha-health-circle.medium { background: rgba(255, 193, 7, 0.15); color: #856404; border: 4px solid var(--fha-warning); }
.fha-health-circle.poor { background: rgba(220, 53, 69, 0.15); color: var(--fha-danger); border: 4px solid var(--fha-danger); }

.fha-score-badge {
  display: inline-block;
  padding: 8px 16px;
  border-radius: 20px;
  font-weight: 600;
  font-size: 18px;
}

.fha-score-success { background: rgba(40, 167, 69, 0.15); color: var(--fha-success); }
.fha-score-warning { background: rgba(255, 193, 7, 0.15); color: #856404; }
.fha-score-danger { background: rgba(220, 53, 69, 0.15); color: var(--fha-danger); }

/* ============================================
   SECTIONS
   ============================================ */
.fha-section {
  margin-bottom: 20px;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  overflow: hidden;
  background: white;
}

.fha-section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 20px;
  background: linear-gradient(135deg, var(--fha-gray-100) 0%, var(--fha-gray-200) 100%);
  cursor: pointer;
  transition: var(--fha-transition);
}

.fha-section-header:hover { background: linear-gradient(135deg, var(--fha-gray-200) 0%, #dee2e6 100%); }

.fha-section-title { display: flex; align-items: center; gap: 10px; }
.fha-section-title i { color: var(--fha-primary); font-size: 18px; }
.fha-section-title h5 { margin: 0; font-size: 15px; font-weight: 600; color: var(--fha-gray-800); }

.fha-section-count { background: var(--fha-gray-500); color: white; padding: 2px 10px; border-radius: 12px; font-size: 12px; font-weight: 600; }
.fha-section-detail { font-size: 12px; color: var(--fha-gray-500); font-weight: normal; }
.fha-section-actions { display: flex; align-items: center; gap: 15px; }
.fha-section-actions > i { color: var(--fha-gray-500); font-size: 14px; }

.fha-section-issues, .fha-section-overview { border-top: 1px solid var(--fha-gray-200); }
.fha-section-overview { background: #fafbfc; padding: 15px 20px; }
.fha-overview-header { font-size: 13px; font-weight: 600; color: var(--fha-gray-500); margin-bottom: 12px; text-transform: uppercase; letter-spacing: 0.5px; }

/* ============================================
   ISSUES
   ============================================ */
.fha-issue-list { list-style: none; padding: 0; margin: 0; }

.fha-issue-item {
  display: flex;
  align-items: flex-start;
  padding: 12px 20px;
  border-bottom: 1px solid #f0f0f0;
  transition: var(--fha-transition);
}

.fha-issue-item:last-child { border-bottom: none; }
.fha-issue-item:hover { background: var(--fha-gray-100); }
.fha-issue-item.clickable { cursor: pointer; }
.fha-issue-item.clickable:hover { background: #e9f5ff; }

.fha-issue-severity {
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  margin-right: 12px;
  min-width: 60px;
  text-align: center;
}

.fha-severity-high { background: var(--fha-danger); color: white; }
.fha-severity-medium { background: var(--fha-warning); color: #333; }
.fha-severity-low { background: var(--fha-info); color: white; }

.fha-issue-content { flex: 1; }
.fha-issue-code { font-weight: 600; color: var(--fha-gray-800); }
.fha-issue-message { color: var(--fha-gray-500); margin-top: 4px; font-size: 14px; }
.fha-issue-actions { display: flex; gap: 8px; margin-left: auto; flex-shrink: 0; }

.fha-issue-link {
  padding: 6px 12px;
  background: var(--fha-primary);
  color: white;
  border-radius: 4px;
  font-size: 12px;
  text-decoration: none;
  transition: var(--fha-transition);
  display: flex;
  align-items: center;
  gap: 5px;
  white-space: nowrap;
}

.fha-issue-link:hover { background: var(--fha-primary-dark); color: white; transform: translateY(-1px); }
.fha-issue-link-list { background: var(--fha-success); }
.fha-issue-link-list:hover { background: #1e7e34; }

/* Severity badges */
.fha-severity-summary { display: flex; gap: 15px; margin-bottom: 20px; flex-wrap: wrap; }
.fha-severity-badge { display: flex; align-items: center; gap: 8px; padding: 8px 15px; border-radius: 20px; font-weight: 600; font-size: 14px; }
.fha-severity-badge.high { background: rgba(220, 53, 69, 0.1); color: var(--fha-danger); }
.fha-severity-badge.medium { background: rgba(255, 193, 7, 0.15); color: #856404; }
.fha-severity-badge.low { background: rgba(23, 162, 184, 0.1); color: var(--fha-info); }
.fha-severity-badge i { font-size: 12px; }

/* ============================================
   TABS
   ============================================ */
.fha-tabs-nav, .fha-view-tabs {
  display: flex;
  border-bottom: 2px solid var(--fha-gray-200);
  margin-bottom: 20px;
  gap: 5px;
}

.fha-tab-btn, .fha-view-tab {
  padding: 12px 24px;
  background: transparent;
  border: none;
  color: var(--fha-gray-500);
  font-weight: 500;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  transition: var(--fha-transition);
}

.fha-tab-btn:hover, .fha-view-tab:hover { color: var(--fha-primary); background: var(--fha-gray-100); }
.fha-tab-btn.active, .fha-view-tab.active { color: var(--fha-primary); border-bottom-color: var(--fha-primary); font-weight: 600; }
.fha-tab-btn .badge { margin-left: 8px; background: var(--fha-primary); color: white; padding: 3px 8px; border-radius: 10px; font-size: 11px; }

.fha-tab-content { animation: fha-fadeIn 0.2s ease-in-out; }

/* ============================================
   TABLES
   ============================================ */
.fha-data-table, .fha-overview-table, .fha-recent-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.fha-data-table th, .fha-overview-table th, .fha-recent-table th {
  text-align: left;
  padding: 10px 12px;
  background: var(--fha-gray-100);
  font-weight: 600;
  color: #495057;
  border-bottom: 2px solid var(--fha-gray-200);
  font-size: 11px;
  text-transform: uppercase;
}

.fha-data-table td, .fha-overview-table td, .fha-recent-table td {
  padding: 10px 12px;
  border-bottom: 1px solid var(--fha-gray-200);
  vertical-align: middle;
}

.fha-data-table tr:hover, .fha-overview-table tr:hover, .fha-recent-table tr:hover { background: var(--fha-gray-100); }

.fha-data-table code, .fha-overview-table code { background: var(--fha-gray-200); padding: 2px 6px; border-radius: 3px; font-size: 12px; }
.fha-data-table a, .fha-overview-table a { color: var(--fha-primary); text-decoration: none; cursor: pointer; }
.fha-data-table a:hover, .fha-overview-table a:hover { text-decoration: underline; }

.fha-row-inactive { opacity: 0.6; }
.fha-row-warning { background: rgba(255, 193, 7, 0.1); }
.fha-row-danger { background: rgba(220, 53, 69, 0.1); }

/* ============================================
   BADGES
   ============================================ */
.fha-status-badge {
  padding: 3px 8px;
  border-radius: 3px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
}

.fha-status-badge.active { background: rgba(40, 167, 69, 0.15); color: var(--fha-success); }
.fha-status-badge.inactive { background: rgba(108, 117, 125, 0.15); color: var(--fha-gray-500); }

.fha-type-badge {
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
  display: inline-block;
  background: var(--fha-gray-200);
  color: #495057;
}

/* Type-specific colors */
.fha-type-badge.type-scheduled-job { background: #6f42c1; color: white; }
.fha-type-badge.type-flow { background: #0d6efd; color: white; }
.fha-type-badge.type-workflow { background: #0dcaf0; color: #000; }
.fha-type-badge.type-notification { background: #fd7e14; color: white; }
.fha-type-badge.type-ui-action { background: #20c997; color: white; }
.fha-type-badge.type-data-source { background: #198754; color: white; }
.fha-type-badge.type-transform-map { background: #6610f2; color: white; }
.fha-type-badge.type-default { background: var(--fha-gray-500); color: white; }

/* ============================================
   FILL RATE
   ============================================ */
.fha-fill-rate { display: flex; align-items: center; gap: 10px; }
.fha-fill-bar { height: 8px; border-radius: 4px; min-width: 5px; max-width: 100px; background: var(--fha-success); }
.fha-fill-bar.warning { background: var(--fha-warning); }
.fha-fill-bar.danger { background: var(--fha-danger); }
.fha-fill-bar.success { background: var(--fha-success); }
.fha-fill-rate span { font-size: 12px; color: var(--fha-gray-500); min-width: 35px; }

/* ============================================
   BUTTONS
   ============================================ */
.fha-btn {
  padding: 10px 20px;
  border-radius: 6px;
  font-weight: 500;
  cursor: pointer;
  transition: var(--fha-transition);
  border: none;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

.fha-btn-primary { background: var(--fha-primary); color: white; }
.fha-btn-primary:hover { background: var(--fha-primary-dark); transform: translateY(-1px); }
.fha-btn-primary:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }

.fha-btn-secondary { background: var(--fha-gray-500); color: white; }
.fha-btn-secondary:hover { background: #545b62; }

.fha-btn-success { background: var(--fha-success); color: white; }
.fha-btn-success:hover { background: #1e7e34; }

.fha-header-link {
  padding: 6px 12px;
  background: var(--fha-primary);
  color: white !important;
  border-radius: 4px;
  font-size: 12px;
  text-decoration: none !important;
  transition: var(--fha-transition);
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 5px;
}

.fha-header-link:hover { background: var(--fha-primary-dark); transform: translateY(-1px); }
.fha-header-link-green { background: var(--fha-success); }
.fha-header-link-green:hover { background: #1e7e34; }

.fha-view-all-link { font-size: 13px; color: var(--fha-primary); text-decoration: none; font-weight: 500; transition: var(--fha-transition); cursor: pointer; }
.fha-view-all-link:hover { color: var(--fha-primary-dark); }
.fha-view-all-link i { margin-left: 4px; font-size: 11px; }

.fha-mini-link { color: var(--fha-primary); cursor: pointer; padding: 4px 8px; border-radius: 3px; transition: var(--fha-transition); }
.fha-mini-link:hover { background: #e9f5ff; color: var(--fha-primary-dark); }

/* ============================================
   FORMS
   ============================================ */
.fha-select {
  width: 100%;
  padding: 12px 15px;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  font-size: 15px;
  transition: var(--fha-transition);
}

.fha-select:focus { border-color: var(--fha-primary); outline: none; }

/* Filter bar */
.fha-filter-bar {
  display: flex;
  gap: 15px;
  align-items: center;
  flex-wrap: wrap;
  padding: 15px 20px;
  background: var(--fha-gray-100);
  border-bottom: 1px solid var(--fha-gray-200);
}

.fha-filter-group { display: flex; align-items: center; gap: 8px; }
.fha-filter-group label { font-size: 12px; font-weight: 600; color: #495057; margin: 0; }
.fha-filter-group select, .fha-filter-group input { padding: 6px 12px; border: 1px solid #ced4da; border-radius: 4px; font-size: 13px; min-width: 120px; }
.fha-filter-group select:focus, .fha-filter-group input:focus { outline: none; border-color: var(--fha-primary); box-shadow: 0 0 0 2px rgba(3, 120, 190, 0.15); }

.fha-filter-search { flex: 1; min-width: 200px; }
.fha-filter-search input { width: 100%; }

.fha-filter-reset { padding: 6px 12px; background: transparent; border: 1px solid var(--fha-gray-500); color: var(--fha-gray-500); border-radius: 4px; font-size: 12px; cursor: pointer; transition: var(--fha-transition); }
.fha-filter-reset:hover { background: var(--fha-gray-500); color: white; }

.fha-filter-count { font-size: 12px; color: var(--fha-gray-500); margin-left: auto; }

/* ============================================
   METADATA
   ============================================ */
.fha-metadata { display: flex; gap: 20px; font-size: 13px; color: rgba(255, 255, 255, 0.9); margin-top: 15px; }
.fha-metadata i { margin-right: 5px; opacity: 0.8; }

/* ============================================
   LOADING &amp; STATES
   ============================================ */
.fha-loading { display: inline-flex; align-items: center; color: var(--fha-primary); margin-left: 15px; }
.fha-loading i { margin-right: 8px; }

.fha-loading-spinner {
  display: inline-block;
  width: 50px;
  height: 50px;
  border: 3px solid var(--fha-primary-light);
  border-top-color: var(--fha-primary);
  border-radius: 50%;
  animation: fha-spin 1s linear infinite;
}

.fha-no-issues { text-align: center; padding: 30px; color: var(--fha-success); }
.fha-no-issues i { font-size: 48px; margin-bottom: 15px; }

/* ============================================
   ANIMATIONS
   ============================================ */
@keyframes fha-spin { to { transform: rotate(360deg); } }
@keyframes fha-fadeIn { from { opacity: 0; } to { opacity: 1; } }

/* ============================================
   RESPONSIVE
   ============================================ */
@media (max-width: 992px) {
  .fha-stats-row, .fha-stats-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .fha-header-container { padding: 0 16px; }
  .fha-nav { position: absolute; top: 64px; left: 0; right: 0; background: #1a1a2e; flex-direction: column; padding: 16px; gap: 4px; display: none; border-top: 1px solid rgba(255, 255, 255, 0.1); box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3); }
  .fha-nav.mobile-open { display: flex; }
  .fha-nav-item { width: 100%; justify-content: flex-start; padding: 14px 16px; }
  .fha-mobile-toggle { display: block; }
  .fha-user-section { display: none; }
  .fha-logo-subtitle { display: none; }
  .fha-stats-row, .fha-stats-grid { grid-template-columns: 1fr; }
  .fha-dashboard, .fha-results-page { padding: 10px; }
}

/* ============================================
   PRINT STYLES
   ============================================ */
@media print {
  .fha-header, .fha-nav, .fha-btn, .fha-tabs-nav, .fha-filter-bar { display: none !important; }
  .fha-card { box-shadow: none; border: 1px solid #000; }
}
</css_variables>
        <footer/>
        <footer_fixed>true</footer_fixed>
        <header display_value="FHA Header">b130c79483deb21083e1b4a6feaad37a</header>
        <matching_now_experience_theme/>
        <name>FHA Theme</name>
        <navbar_fixed>true</navbar_fixed>
        <sys_class_name>sp_theme</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-01-02 19:36:30</sys_created_on>
        <sys_id>1e95d9c48392321083e1b4a6feaad356</sys_id>
        <sys_mod_count>2</sys_mod_count>
        <sys_name>FHA Theme</sys_name>
        <sys_package display_value="Foundation Health Analyzer" source="x_1310794_founda_0">d852994c8312321083e1b4a6feaad3e6</sys_package>
        <sys_policy/>
        <sys_scope display_value="Foundation Health Analyzer">d852994c8312321083e1b4a6feaad3e6</sys_scope>
        <sys_update_name>sp_theme_1e95d9c48392321083e1b4a6feaad356</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-01-03 22:01:39</sys_updated_on>
        <turn_off_scss_compilation>false</turn_off_scss_compilation>
    </sp_theme>
</record_update>
