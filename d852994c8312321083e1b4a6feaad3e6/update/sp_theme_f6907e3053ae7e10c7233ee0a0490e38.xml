<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_theme">
    <sp_theme action="INSERT_OR_UPDATE">
        <css_variables/>
        /* ============================================
   FOUNDATION HEALTH ANALYZER - UNIFIED THEME
   Version: 1.3.0
   ============================================ */

/* ============================================
   1. CSS VARIABLES
   ============================================ */
:root {
  /* Couleurs principales */
  --fha-primary: #0378BE;
  --fha-primary-dark: #035B8E;
  --fha-primary-light: #E3F2FD;

  /* Couleurs sémantiques */
  --fha-success: #28A745;
  --fha-warning: #FFC107;
  --fha-danger: #DC3545;
  --fha-info: #17A2B8;

  /* Niveaux de gris */
  --fha-gray-100: #F8F9FA;
  --fha-gray-200: #E9ECEF;
  --fha-gray-300: #DEE2E6;
  --fha-gray-500: #6C757D;
  --fha-gray-800: #343A40;
  --fha-gray-900: #212529;

  /* Backgrounds spéciaux */
  --fha-header-bg: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  --fha-header-accent: #00d4ff;

  /* Espacements */
  --fha-spacing-xs: 4px;
  --fha-spacing-sm: 8px;
  --fha-spacing-md: 16px;
  --fha-spacing-lg: 24px;
  --fha-spacing-xl: 32px;

  /* Border radius */
  --fha-radius: 8px;
  --fha-radius-lg: 10px;
  --fha-radius-xl: 12px;

  /* Shadows */
  --fha-shadow: 0 2px 10px rgba(0, 0, 0, 0.08);
  --fha-shadow-lg: 0 4px 20px rgba(0, 0, 0, 0.15);
  --fha-shadow-xl: 0 8px 30px rgba(0, 0, 0, 0.2);

  /* Transitions */
  --fha-transition: all 0.2s ease;
  --fha-transition-fast: all 0.15s ease;
  --fha-transition-slow: all 0.3s ease;
}

/* ============================================
   2. LAYOUT & CONTAINERS
   ============================================ */

/* Container principal */
.fha-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 var(--fha-spacing-lg);
}

.fha-container-fluid {
  width: 100%;
  padding: 0 var(--fha-spacing-lg);
}

/* Sections */
.fha-section {
  margin-bottom: var(--fha-spacing-lg);
  padding: var(--fha-spacing-xl);
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  border: 1px solid var(--fha-gray-200);
}

/* ============================================
   3. HEADERS
   ============================================ */

.fha-header {
  background: var(--fha-header-bg);
  padding: 0;
  box-shadow: var(--fha-shadow-xl);
  position: sticky;
  top: 0;
  z-index: 1000;
}

.fha-header-container {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 var(--fha-spacing-lg);
  height: 64px;
}

.fha-page-header {
  background: var(--fha-header-bg);
  color: white;
  padding: var(--fha-spacing-xl) var(--fha-spacing-lg);
  margin-bottom: var(--fha-spacing-lg);
  box-shadow: var(--fha-shadow-xl);
  border-radius: var(--fha-radius-lg);
}

.fha-page-header h1,
.fha-page-header h2 {
  margin: 0 0 var(--fha-spacing-sm) 0;
  font-weight: 600;
  color: white;
}

/* ============================================
   4. CARDS
   ============================================ */

.fha-card {
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  overflow: hidden;
  transition: var(--fha-transition);
  border: 1px solid var(--fha-gray-200);
}

.fha-card:hover {
  box-shadow: var(--fha-shadow-lg);
  transform: translateY(-2px);
}

.fha-card-header {
  background: var(--fha-gray-100);
  padding: 15px 20px;
  border-bottom: 1px solid var(--fha-gray-200);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.fha-card-header h4 {
  margin: 0;
  font-size: 16px;
  font-weight: 600;
  color: var(--fha-gray-800);
}

.fha-card-body {
  padding: 20px;
}

.fha-card-footer {
  background: var(--fha-gray-100);
  padding: 15px 20px;
  border-top: 1px solid var(--fha-gray-200);
}

/* Grille de cards */
.fha-card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: var(--fha-spacing-lg);
  margin: var(--fha-spacing-lg) 0;
}

/* ============================================
   5. BUTTONS
   ============================================ */

.fha-btn {
  display: inline-flex;
  align-items: center;
  gap: var(--fha-spacing-sm);
  padding: 12px 24px;
  border: none;
  border-radius: var(--fha-radius-lg);
  font-weight: 500;
  font-size: 15px;
  cursor: pointer;
  transition: var(--fha-transition);
  text-decoration: none;
  line-height: 1;
}

.fha-btn-primary {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  box-shadow: 0 2px 8px rgba(3, 120, 190, 0.3);
}

.fha-btn-primary:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(3, 120, 190, 0.4);
}

.fha-btn-secondary {
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
  border: 1px solid var(--fha-gray-300);
}

.fha-btn-secondary:hover {
  background: var(--fha-gray-200);
}

.fha-btn-success {
  background: var(--fha-success);
  color: white;
}

.fha-btn-danger {
  background: var(--fha-danger);
  color: white;
}

.fha-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.fha-btn-sm {
  padding: 8px 16px;
  font-size: 14px;
}

.fha-btn-lg {
  padding: 16px 32px;
  font-size: 16px;
}

/* ============================================
   6. FORMS & INPUTS
   ============================================ */

.fha-input,
.fha-select {
  width: 100%;
  padding: 12px 15px;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius-lg);
  font-size: 15px;
  transition: var(--fha-transition);
  background: white;
}

.fha-input:focus,
.fha-select:focus {
  border-color: var(--fha-primary);
  outline: none;
  box-shadow: 0 0 0 3px rgba(3, 120, 190, 0.15);
}

/* ============================================
   7. STATS & METRICS
   ============================================ */

.fha-stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--fha-spacing-lg);
  margin: var(--fha-spacing-lg) 0;
}

.fha-stat-card {
  background: white;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  padding: 20px;
  text-align: center;
  transition: var(--fha-transition);
}

.fha-stat-card:hover {
  border-color: var(--fha-primary);
  box-shadow: var(--fha-shadow);
}

.fha-stat-number {
  font-size: 32px;
  font-weight: 700;
  color: var(--fha-primary);
  margin: 0 0 var(--fha-spacing-sm) 0;
}

.fha-stat-label {
  font-size: 13px;
  color: var(--fha-gray-500);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* Health Score Circle */
.fha-health-circle {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto var(--fha-spacing-md);
  font-size: 32px;
  font-weight: 700;
}

.fha-health-circle.good {
  background: rgba(40, 167, 69, 0.15);
  color: var(--fha-success);
  border: 4px solid var(--fha-success);
}

.fha-health-circle.medium {
  background: rgba(255, 193, 7, 0.15);
  color: #856404;
  border: 4px solid var(--fha-warning);
}

.fha-health-circle.poor {
  background: rgba(220, 53, 69, 0.15);
  color: var(--fha-danger);
  border: 4px solid var(--fha-danger);
}

/* ============================================
   8. BADGES & LABELS
   ============================================ */

.fha-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  white-space: nowrap;
}

.fha-badge-success {
  background: rgba(40, 167, 69, 0.15);
  color: var(--fha-success);
}

.fha-badge-warning {
  background: rgba(255, 193, 7, 0.15);
  color: #856404;
}

.fha-badge-danger {
  background: rgba(220, 53, 69, 0.15);
  color: var(--fha-danger);
}

.fha-badge-info {
  background: var(--fha-primary-light);
  color: var(--fha-primary);
}

/* ============================================
   9. TABLES
   ============================================ */

.fha-table {
  width: 100%;
  border-collapse: collapse;
  margin: var(--fha-spacing-lg) 0;
  font-size: 14px;
  border-radius: var(--fha-radius);
  overflow: hidden;
  box-shadow: var(--fha-shadow);
}

.fha-table thead {
  background: var(--fha-gray-100);
}

.fha-table th {
  padding: 12px 16px;
  text-align: left;
  font-weight: 600;
  color: var(--fha-gray-800);
  border-bottom: 2px solid var(--fha-gray-300);
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.fha-table td {
  padding: 12px 16px;
  border-bottom: 1px solid var(--fha-gray-200);
}

.fha-table tbody tr:hover {
  background: var(--fha-gray-100);
}

.fha-table tbody tr:last-child td {
  border-bottom: none;
}

/* ============================================
   10. CODE BLOCKS
   ============================================ */

.fha-code-inline {
  background: var(--fha-gray-100);
  color: var(--fha-primary);
  padding: 2px 8px;
  border-radius: 4px;
  font-family: 'Monaco', 'Menlo', 'Courier New', monospace;
  font-size: 13px;
  font-weight: 500;
}

.fha-code-block {
  background: #2d2d2d;
  color: #e2e8f0;
  padding: 20px;
  border-radius: var(--fha-radius);
  font-family: 'Monaco', 'Menlo', 'Courier New', monospace;
  font-size: 13px;
  overflow-x: auto;
  margin: var(--fha-spacing-md) 0;
  line-height: 1.6;
  box-shadow: var(--fha-shadow);
}

/* ============================================
   11. INFO BOXES
   ============================================ */

.fha-info-box {
  background: var(--fha-primary-light);
  border-left: 4px solid var(--fha-primary);
  padding: var(--fha-spacing-md) 20px;
  border-radius: 0 var(--fha-radius) var(--fha-radius) 0;
  margin: var(--fha-spacing-lg) 0;
}

.fha-info-box.success {
  background: rgba(40, 167, 69, 0.1);
  border-left-color: var(--fha-success);
}

.fha-info-box.warning {
  background: rgba(255, 193, 7, 0.1);
  border-left-color: var(--fha-warning);
}

.fha-info-box.danger {
  background: rgba(220, 53, 69, 0.1);
  border-left-color: var(--fha-danger);
}

.fha-info-box p:last-child {
  margin-bottom: 0;
}

/* ============================================
   12. NAVIGATION
   ============================================ */

.fha-nav {
  background: white;
  border-bottom: 1px solid var(--fha-gray-200);
  position: sticky;
  top: 64px;
  z-index: 999;
  box-shadow: var(--fha-shadow);
}

.fha-nav-items {
  display: flex;
  gap: 4px;
  padding: 12px 0;
  min-width: max-content;
}

.fha-nav-btn {
  padding: 8px 16px;
  border: none;
  background: transparent;
  color: var(--fha-gray-500);
  border-radius: var(--fha-radius);
  cursor: pointer;
  font-size: 14px;
  font-weight: 500;
  transition: var(--fha-transition);
  white-space: nowrap;
}

.fha-nav-btn:hover {
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
}

.fha-nav-btn.active {
  background: var(--fha-primary);
  color: white;
  font-weight: 600;
}

/* ============================================
   13. UTILITIES
   ============================================ */

.fha-text-center { text-align: center; }
.fha-text-right { text-align: right; }
.fha-text-muted { color: var(--fha-gray-500); }
.fha-text-primary { color: var(--fha-primary); }
.fha-text-success { color: var(--fha-success); }
.fha-text-warning { color: var(--fha-warning); }
.fha-text-danger { color: var(--fha-danger); }

.fha-mb-0 { margin-bottom: 0; }
.fha-mb-sm { margin-bottom: var(--fha-spacing-sm); }
.fha-mb-md { margin-bottom: var(--fha-spacing-md); }
.fha-mb-lg { margin-bottom: var(--fha-spacing-lg); }

.fha-mt-0 { margin-top: 0; }
.fha-mt-sm { margin-top: var(--fha-spacing-sm); }
.fha-mt-md { margin-top: var(--fha-spacing-md); }
.fha-mt-lg { margin-top: var(--fha-spacing-lg); }

/* ============================================
   14. RESPONSIVE
   ============================================ */

@media (max-width: 768px) {
  .fha-container,
  .fha-container-fluid {
    padding: 0 var(--fha-spacing-md);
  }

  .fha-page-header {
    padding: var(--fha-spacing-lg) var(--fha-spacing-md);
  }

  .fha-section {
    padding: var(--fha-spacing-lg);
  }

  .fha-card-grid,
  .fha-stats-grid {
    grid-template-columns: 1fr;
  }

  .fha-table {
    font-size: 12px;
  }

  .fha-table th,
  .fha-table td {
    padding: 10px 12px;
  }
}

/* ============================================
   15. ANIMATIONS
   ============================================ */

@keyframes fha-fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fha-fade-in {
  animation: fha-fadeIn 0.3s ease-out;
}

/* ============================================
   WIDGET: FHA Dashboard
   ============================================ */
/* FHA Dashboard Widget Styles */
.fha-dashboard {
  padding: 20px;
}

.fha-page-header {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  padding: 30px;
  border-radius: var(--fha-radius-lg);
  margin-bottom: 25px;
  box-shadow: var(--fha-shadow);
}

.fha-page-header h2 {
  margin: 0 0 10px 0;
  font-weight: 600;
}

.fha-page-header p {
  margin: 0;
  opacity: 0.9;
}

.fha-card {
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  margin-bottom: 20px;
  overflow: hidden;
  transition: var(--fha-transition);
}

.fha-card:hover {
  box-shadow: var(--fha-shadow-lg);
}

.fha-card-header {
  background: var(--fha-gray-100);
  padding: 15px 20px;
  border-bottom: 1px solid var(--fha-gray-200);
}

.fha-card-header h4 {
  margin: 0;
  font-size: 16px;
  font-weight: 600;
  color: var(--fha-gray-800);
}

.fha-card-body {
  padding: 20px;
}

.fha-select {
  width: 100%;
  padding: 12px 15px;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  font-size: 15px;
  transition: var(--fha-transition);
  background: white;
}

.fha-select:focus {
  border-color: var(--fha-primary);
  outline: none;
  box-shadow: var(--fha-shadow);
}

.fha-btn-primary {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  border: none;
  padding: 12px 30px;
  border-radius: var(--fha-radius-lg);
  font-size: 15px;
  font-weight: 500;
  cursor: pointer;
  transition: var(--fha-transition);
  box-shadow: var(--fha-shadow);
}

.fha-btn-primary:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-btn-primary:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.fha-loading {
  display: inline-flex;
  align-items: center;
  color: var(--fha-primary);
  margin-left: 15px;
}

.fha-loading i {
  margin-right: 8px;
}

.fha-score-badge {
  display: inline-block;
  padding: 8px 16px;
  border-radius: var(--fha-radius-lg);
  font-weight: 600;
  font-size: 16px;
}

.fha-score-success {
  background: var(--fha-success);
  color: white;
}

.fha-score-warning {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-score-danger {
  background: var(--fha-danger);
  color: white;
}

.fha-issue-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.fha-issue-item {
  display: flex;
  align-items: flex-start;
  padding: 15px 20px;
  border-bottom: 1px solid var(--fha-gray-200);
  transition: var(--fha-transition);
}

.fha-issue-item:last-child {
  border-bottom: none;
}

.fha-issue-item:hover {
  background: var(--fha-gray-100);
}

.fha-issue-item.clickable {
  cursor: pointer;
}

.fha-issue-item.clickable:hover {
  background: var(--fha-primary-light);
}

.fha-issue-severity {
  padding: 4px 12px;
  border-radius: var(--fha-radius);
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  margin-right: 12px;
  min-width: 65px;
  text-align: center;
}

.fha-severity-high {
  background: var(--fha-danger);
  color: white;
}

.fha-severity-medium {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-severity-low {
  background: var(--fha-info);
  color: white;
}

.fha-issue-content {
  flex: 1;
}

.fha-issue-code {
  font-weight: 600;
  color: var(--fha-gray-800);
}

.fha-issue-message {
  color: var(--fha-gray-500);
  margin-top: 4px;
  font-size: 14px;
}

.fha-recent-table {
  width: 100%;
  border-collapse: collapse;
}

.fha-recent-table th,
.fha-recent-table td {
  padding: 12px;
  text-align: left;
  border-bottom: 1px solid var(--fha-gray-200);
}

.fha-recent-table th {
  background: var(--fha-gray-100);
  font-weight: 600;
  color: var(--fha-gray-500);
  font-size: 12px;
  text-transform: uppercase;
}

.fha-recent-table tr:hover {
  background: var(--fha-gray-100);
}

.fha-stats-row {
  display: flex;
  gap: 20px;
  margin-bottom: 20px;
}

.fha-stat-box {
  flex: 1;
  background: linear-gradient(135deg, var(--fha-gray-100) 0%, var(--fha-gray-200) 100%);
  padding: 20px;
  border-radius: var(--fha-radius-lg);
  text-align: center;
}

.fha-stat-value {
  font-size: 28px;
  font-weight: 700;
  color: var(--fha-primary);
}

.fha-stat-label {
  font-size: 13px;
  color: var(--fha-gray-500);
  margin-top: 5px;
}

.fha-no-issues {
  text-align: center;
  padding: 30px;
  color: var(--fha-success);
}

.fha-no-issues i {
  font-size: 48px;
  margin-bottom: 15px;
}

/* Section Styles */
.fha-section {
  margin-bottom: 20px;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius-lg);
  overflow: hidden;
  background: white;
}

.fha-section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 20px;
  background: linear-gradient(135deg, var(--fha-gray-100) 0%, var(--fha-gray-200) 100%);
  cursor: pointer;
  transition: var(--fha-transition);
}

.fha-section-header:hover {
  background: linear-gradient(135deg, var(--fha-gray-200) 0%, var(--fha-gray-100) 100%);
}

.fha-section-title {
  display: flex;
  align-items: center;
  gap: 10px;
}

.fha-section-title i {
  color: var(--fha-primary);
  font-size: 18px;
}

.fha-section-title h5 {
  margin: 0;
  font-size: 15px;
  font-weight: 600;
  color: var(--fha-gray-800);
}

.fha-section-count {
  background: var(--fha-gray-500);
  color: white;
  padding: 3px 12px;
  border-radius: var(--fha-radius-lg);
  font-size: 12px;
  font-weight: 600;
}

.fha-section-detail {
  font-size: 12px;
  color: var(--fha-gray-500);
  font-weight: normal;
}

.fha-section-actions {
  display: flex;
  align-items: center;
  gap: 15px;
}

.fha-section-actions &gt; i {
  color: var(--fha-gray-500);
  font-size: 14px;
}

.fha-header-link {
  padding: 6px 14px;
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white !important;
  border-radius: var(--fha-radius);
  font-size: 12px;
  text-decoration: none !important;
  transition: var(--fha-transition);
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 5px;
  box-shadow: var(--fha-shadow);
}

.fha-header-link:hover {
  transform: translateY(-1px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-header-link-green {
  background: linear-gradient(135deg, var(--fha-success) 0%, var(--fha-success) 100%);
  box-shadow: var(--fha-shadow);
}

.fha-header-link-green:hover {
  box-shadow: var(--fha-shadow-lg);
}

/* Section Issues */
.fha-section-issues {
  border-top: 1px solid var(--fha-gray-200);
}

/* Section Overview */
.fha-section-overview {
  border-top: 1px solid var(--fha-gray-200);
  background: var(--fha-gray-100);
  padding: 15px 20px;
}

.fha-overview-header {
  font-size: 13px;
  font-weight: 600;
  color: var(--fha-gray-500);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.fha-overview-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.fha-overview-table th {
  text-align: left;
  padding: 10px 12px;
  background: var(--fha-gray-200);
  font-weight: 600;
  color: var(--fha-gray-800);
  font-size: 11px;
  text-transform: uppercase;
  border-radius: var(--fha-radius);
}

.fha-overview-table td {
  padding: 10px 12px;
  border-bottom: 1px solid var(--fha-gray-200);
  vertical-align: middle;
}

.fha-overview-table tr:hover {
  background: var(--fha-primary-light);
}

.fha-overview-table tr.fha-row-inactive {
  opacity: 0.6;
}

.fha-overview-table tr.fha-row-warning {
  background: var(--fha-warning);
}

.fha-overview-table tr.fha-row-danger {
  background: var(--fha-danger);
}

.fha-overview-table a {
  color: var(--fha-primary);
  text-decoration: none;
  cursor: pointer;
}

.fha-overview-table a:hover {
  text-decoration: underline;
}

.fha-overview-table code {
  background: var(--fha-gray-200);
  padding: 2px 6px;
  border-radius: var(--fha-radius);
  font-size: 12px;
}

/* Status and Type Badges */
.fha-status-badge {
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
}

.fha-status-badge.active {
  background: var(--fha-success);
  color: white;
}

.fha-status-badge.inactive {
  background: var(--fha-gray-500);
  color: white;
}

.fha-type-badge {
  background: var(--fha-gray-200);
  color: var(--fha-gray-800);
  padding: 4px 10px;
  border-radius: var(--fha-radius);
  font-size: 11px;
  font-weight: 500;
}

/* Fill Rate Bar */
.fha-fill-rate {
  display: flex;
  align-items: center;
  gap: 10px;
}

.fha-fill-bar {
  height: 8px;
  border-radius: var(--fha-radius);
  background: var(--fha-success);
  min-width: 5px;
  max-width: 100px;
}

.fha-fill-bar.danger {
  background: var(--fha-danger);
}

.fha-fill-bar.warning {
  background: var(--fha-warning);
}

.fha-fill-bar.success {
  background: var(--fha-success);
}

.fha-fill-rate span {
  font-size: 12px;
  color: var(--fha-gray-500);
  min-width: 35px;
}

/* Mini Link */
.fha-mini-link {
  color: var(--fha-primary);
  cursor: pointer;
  padding: 4px 8px;
  border-radius: var(--fha-radius);
  transition: var(--fha-transition);
}

.fha-mini-link:hover {
  background: var(--fha-primary-light);
  color: var(--fha-primary-dark);
}

/* Severity Summary */
.fha-severity-summary {
  display: flex;
  gap: 15px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.fha-severity-badge {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 15px;
  border-radius: var(--fha-radius-lg);
  font-weight: 600;
  font-size: 14px;
}

.fha-severity-badge.high {
  background: var(--fha-danger);
  color: white;
}

.fha-severity-badge.medium {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-severity-badge.low {
  background: var(--fha-info);
  color: white;
}

.fha-severity-badge i {
  font-size: 12px;
}

/* View All link */
.fha-view-all-link {
  font-size: 13px;
  color: var(--fha-primary);
  text-decoration: none;
  font-weight: 500;
  transition: var(--fha-transition);
  cursor: pointer;
}

.fha-view-all-link:hover {
  color: var(--fha-primary-dark);
  text-decoration: none;
}

.fha-view-all-link i {
  margin-left: 4px;
  font-size: 11px;
}

/* Analysis Options */
.fha-option-enabled {
  background: var(--fha-success);
  color: white;
  border: 1px solid var(--fha-success);
}

.fha-option-disabled {
  background: var(--fha-gray-100);
  color: var(--fha-gray-500);
  border: 1px solid var(--fha-gray-200);
}

/* Issue Actions */
.fha-issue-actions {
  display: flex;
  gap: 8px;
  margin-left: auto;
  flex-shrink: 0;
}

.fha-issue-link {
  padding: 6px 12px;
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  border-radius: var(--fha-radius);
  font-size: 12px;
  text-decoration: none;
  transition: var(--fha-transition);
  display: flex;
  align-items: center;
  gap: 5px;
  white-space: nowrap;
}

.fha-issue-link:hover {
  color: white;
  text-decoration: none;
  transform: translateY(-1px);
}

.fha-issue-link-list {
  background: linear-gradient(135deg, var(--fha-success) 0%, var(--fha-success) 100%);
}

/* ============================================
   WIDGET: FHA Analysis Results
   ============================================ */
/* FHA Analysis List Widget Styles */
.fha-analysis-list {
  padding: 20px;
  max-width: 1400px;
  margin: 0 auto;
}

/* Page Header */
.fha-list-header {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
    color: white;
    padding: 40px;
    border-radius: var(--fha-radius-lg);
    margin-bottom: 30px;
    box-shadow: var(--fha-shadow);
}


.fha-list-header h1 {
  margin: 0 0 10px 0;
  font-weight: 600;
}

.fha-list-header p {
  margin: 0;
  opacity: 0.9;
}

.fha-header-stats {
  display: flex;
  gap: 30px;
  margin-top: 20px;
}

.fha-header-stat {
  text-align: center;
}

.fha-header-stat-value {
  font-size: 28px;
  font-weight: 700;
}

.fha-header-stat-label {
  font-size: 12px;
  opacity: 0.8;
  text-transform: uppercase;
}

/* Filter Bar */
.fha-filter-bar {
  background: white;
  border-radius: var(--fha-radius-lg);
  padding: 20px;
  margin-bottom: 20px;
  box-shadow: var(--fha-shadow);
  display: flex;
  gap: 15px;
  align-items: center;
  flex-wrap: wrap;
}

.fha-filter-group {
  display: flex;
  align-items: center;
  gap: 8px;
}

.fha-filter-group label {
  font-size: 13px;
  font-weight: 600;
  color: var(--fha-gray-800);
  margin: 0;
}

.fha-filter-input {
  padding: 8px 12px;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  font-size: 14px;
  min-width: 150px;
  transition: var(--fha-transition);
}

.fha-filter-input:focus {
  border-color: var(--fha-primary);
  outline: none;
  box-shadow: var(--fha-shadow);
}

.fha-search-input {
  flex: 1;
  min-width: 250px;
}

.fha-filter-actions {
  display: flex;
  gap: 10px;
  margin-left: auto;
}

.fha-btn {
  padding: 8px 16px;
  border-radius: var(--fha-radius);
  font-weight: 500;
  cursor: pointer;
  transition: var(--fha-transition);
  border: none;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 14px;
}

.fha-btn-primary {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
}

.fha-btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-btn-secondary {
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
  border: 1px solid var(--fha-gray-200);
}

.fha-btn-secondary:hover {
  background: var(--fha-gray-200);
}

/* Analysis Cards */
.fha-analysis-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
  gap: 20px;
}

.fha-analysis-card {
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  overflow: hidden;
  transition: var(--fha-transition);
  cursor: pointer;
}

.fha-analysis-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-card-header {
  padding: 20px;
  border-bottom: 1px solid var(--fha-gray-200);
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.fha-card-info h3 {
  margin: 0 0 5px 0;
  font-size: 18px;
  font-weight: 600;
  color: var(--fha-gray-800);
}

.fha-card-number {
  font-size: 16px;
  color: var(--fha-gray-800);
  font-weight: 600;
  margin-bottom: 4px;
}

.fha-card-table {
  font-size: 13px;
  color: var(--fha-gray-500);
}


.fha-card-score {
  text-align: center;
}

.fha-score-circle {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  font-weight: 700;
}

.fha-score-circle.good {
  background: var(--fha-success);
  color: white;
  border: 3px solid var(--fha-success);
}

.fha-score-circle.medium {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
  border: 3px solid var(--fha-warning);
}

.fha-score-circle.poor {
  background: var(--fha-danger);
  color: white;
  border: 3px solid var(--fha-danger);
}

.fha-score-label {
  font-size: 11px;
  color: var(--fha-gray-500);
  margin-top: 5px;
}

.fha-card-body {
  padding: 20px;
}

.fha-card-stats {
  display: flex;
  gap: 20px;
  margin-bottom: 15px;
}

.fha-card-stat {
  text-align: center;
  flex: 1;
}

.fha-card-stat-value {
  font-size: 20px;
  font-weight: 700;
  color: var(--fha-gray-800);
}

.fha-card-stat-value.danger { color: var(--fha-danger); }
.fha-card-stat-value.warning { color: var(--fha-warning); }
.fha-card-stat-value.info { color: var(--fha-info); }

.fha-card-stat-label {
  font-size: 11px;
  color: var(--fha-gray-500);
  text-transform: uppercase;
}

.fha-card-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 15px;
  border-top: 1px solid var(--fha-gray-200);
  font-size: 13px;
  color: var(--fha-gray-500);
}

.fha-card-meta i {
  margin-right: 5px;
}

.fha-card-actions {
  display: flex;
  gap: 8px;
}

.fha-card-action {
  padding: 6px 10px;
  border-radius: var(--fha-radius);
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
  border: none;
  cursor: pointer;
  transition: var(--fha-transition);
}

.fha-card-action:hover {
  background: var(--fha-gray-200);
}

.fha-card-action.danger:hover {
  background: var(--fha-danger);
  color: white;
}

/* Empty State */
.fha-empty-state {
  text-align: center;
  padding: 60px 20px;
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
}

.fha-empty-state i {
  font-size: 64px;
  color: var(--fha-gray-200);
  margin-bottom: 20px;
}

.fha-empty-state h3 {
  margin: 0 0 10px 0;
  color: var(--fha-gray-800);
}

.fha-empty-state p {
  color: var(--fha-gray-500);
  margin: 0 0 20px 0;
}

/* Load More */
.fha-load-more {
  text-align: center;
  margin-top: 30px;
}

.fha-results-count {
  font-size: 14px;
  color: var(--fha-gray-500);
  margin-bottom: 15px;
}

/* Loading */
.fha-loading {
  text-align: center;
  padding: 40px;
}

.fha-loading i {
  font-size: 32px;
  color: var(--fha-primary);
}

/* Table View */
.fha-table-view {
  background: white;
  border-radius: var(--fha-radius-lg);
  box-shadow: var(--fha-shadow);
  overflow: hidden;
}

.fha-table {
  width: 100%;
  border-collapse: collapse;
}

.fha-table th {
  background: var(--fha-gray-100);
  padding: 15px;
  text-align: left;
  font-weight: 600;
  color: var(--fha-gray-800);
  font-size: 13px;
  text-transform: uppercase;
  cursor: pointer;
  transition: var(--fha-transition);
}

.fha-table th:hover {
  background: var(--fha-gray-200);
}

.fha-table th i {
  margin-left: 5px;
  opacity: 0.5;
}

.fha-table th.active i {
  opacity: 1;
  color: var(--fha-primary);
}

.fha-table td {
  padding: 15px;
  border-bottom: 1px solid var(--fha-gray-200);
}

.fha-table tr:hover {
  background: var(--fha-gray-100);
  cursor: pointer;
}

.fha-table-score {
  display: inline-block;
  padding: 5px 12px;
  border-radius: 20px;
  font-weight: 600;
  font-size: 14px;
}

.fha-table-score.good {
  background: var(--fha-success);
  color: white;
}

.fha-table-score.medium {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-table-score.poor {
  background: var(--fha-danger);
  color: white;
}

/* View Toggle */
.fha-view-toggle {
  display: flex;
  gap: 5px;
  background: var(--fha-gray-100);
  padding: 4px;
  border-radius: var(--fha-radius);
}

.fha-view-toggle button {
  padding: 8px 12px;
  border: none;
  background: transparent;
  border-radius: var(--fha-radius);
  cursor: pointer;
  transition: var(--fha-transition);
  color: var(--fha-gray-500);
}

.fha-view-toggle button.active {
  background: white;
  color: var(--fha-primary);
  box-shadow: var(--fha-shadow);
}

/* Responsive */
@media (max-width: 768px) {
  .fha-analysis-grid {
    grid-template-columns: 1fr;
  }
  
  .fha-filter-bar {
    flex-direction: column;
    align-items: stretch;
  }
  
  .fha-filter-actions {
    margin-left: 0;
    justify-content: flex-end;
  }
  
  .fha-header-stats {
    flex-wrap: wrap;
  }
}

/* ============================================
   WIDGET: FHA Analysis Detail
   ============================================ */
.fha-detail {
  max-width: 1200px;
  margin: 0 auto;
  padding: 22px 18px 28px;
  background: var(--fha-gray-100);
}

.fha-header {
  background: var(--fha-header-bg);
  color: #fff;
  padding: 22px 24px;
  border-radius: var(--fha-radius-lg);
  margin-bottom: 18px;
  box-shadow: var(--fha-shadow-lg);
}

.fha-header h1 {
  margin: 0;
  font-size: 24px;
  font-weight: 700;
}

.fha-stats {
  display: flex;
  gap: 16px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.fha-stat-card {
  flex: 1;
  min-width: 200px;
  background: white;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  padding: 16px;
  box-shadow: var(--fha-shadow);
  text-align: center;
  transition: var(--fha-transition);
}

.fha-stat-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-stat-value {
  font-size: 28px;
  font-weight: 800;
  line-height: 1;
  margin-bottom: 8px;
}

.fha-stat-value.danger {
  color: var(--fha-danger);
}

.fha-stat-value.warning {
  color: var(--fha-warning);
}

.fha-stat-value.info {
  color: var(--fha-info);
}

.fha-stat-label {
  font-size: 13px;
  font-weight: 600;
  color: var(--fha-gray-500);
  text-transform: uppercase;
  letter-spacing: 0.4px;
}




.fha-meta {
  margin-top: 10px;
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  font-size: 13px;
}

.fha-actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin: 8px 0 16px;
}

.fha-btn {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  border: none;
  border-radius: var(--fha-radius-lg);
  padding: 10px 14px;
  font-weight: 700;
  cursor: pointer;
  box-shadow: var(--fha-shadow);
  transition: var(--fha-transition);
}

.fha-btn i {
  margin-right: 6px;
}

.fha-btn:hover {
  transform: translateY(-1px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 10px;
  border-radius: var(--fha-radius-lg);
  background: var(--fha-primary);
  color: white;
  border: 1px solid var(--fha-primary-dark);
  font-weight: 600;
}

.fha-tabs {
  display: flex;
  gap: 10px;
  margin: 18px 0 14px;
  flex-wrap: wrap;
  border-bottom: 2px solid var(--fha-gray-200);
  padding-bottom: 6px;
}

.fha-tab-btn {
  border: none;
  background: transparent;
  padding: 10px 16px;
  border-radius: var(--fha-radius-lg) var(--fha-radius-lg) 0 0;
  cursor: pointer;
  font-weight: 700;
  color: var(--fha-gray-500);
  border-bottom: 3px solid transparent;
  transition: var(--fha-transition);
}

.fha-tab-btn:hover {
  color: var(--fha-primary);
  background: var(--fha-primary-light);
}

.fha-tab-btn.active {
  color: var(--fha-primary);
  border-bottom-color: var(--fha-primary);
  background: var(--fha-primary-light);
}

.fha-tab-btn .fha-badge {
  background: var(--fha-primary);
  color: white;
  border: none;
  padding: 3px 8px;
  font-size: 11px;
}

.fha-card {
  background: white;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius-lg);
  margin-bottom: 16px;
  box-shadow: var(--fha-shadow);
  overflow: hidden;
}

.fha-card h3 {
  margin: 0;
  font-size: 16px;
  font-weight: 700;
  color: var(--fha-gray-800);
}

.fha-card__head {
  padding: 14px 16px;
  border-bottom: 1px solid var(--fha-gray-200);
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: var(--fha-gray-100);
}

.fha-card__body {
  padding: 16px;
}

.fha-data-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.fha-data-table th,
.fha-data-table td {
  padding: 10px 12px;
  border-bottom: 1px solid var(--fha-gray-200);
  text-align: left;
}

.fha-data-table th {
  background: var(--fha-gray-100);
  font-weight: 700;
  color: var(--fha-gray-800);
}

.fha-data-table tr:hover td {
  background: var(--fha-gray-100);
}

.fha-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 10px;
  border-radius: var(--fha-radius-lg);
  font-size: 12px;
  background: var(--fha-gray-200);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.2px;
}

.fha-pill-red { background: var(--fha-danger); color: white; }
.fha-pill-amber { background: var(--fha-warning); color: var(--fha-gray-800); }
.fha-pill-blue { background: var(--fha-info); color: white; }
.fha-pill-gray { background: var(--fha-gray-500); color: white; }
.fha-pill-cat {
  color: white;
  border: none;
}

.fha-issue-badge {
  padding: 6px 10px;
  border-radius: var(--fha-radius);
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
  display: inline-block;
  letter-spacing: 0.4px;
}
.fha-sev-high { background: var(--fha-danger); color: white; }
.fha-sev-medium { background: var(--fha-warning); color: var(--fha-gray-800); }
.fha-sev-low { background: var(--fha-info); color: white; }

.fha-section-title {
  display: flex;
  align-items: center;
  gap: 8px;
  font-weight: 700;
  margin: 0 0 6px 0;
  color: var(--fha-gray-800);
}

.fha-filter-bar {
  display: flex;
  gap: 10px;
  align-items: center;
  flex-wrap: wrap;
  margin-bottom: 12px;
}

.fha-filter-bar label {
  font-size: 12px;
  font-weight: 700;
  color: var(--fha-gray-500);
}

.fha-filter-bar input,
.fha-filter-bar select {
  padding: 8px 10px;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  font-size: 13px;
  background: white;
  min-width: 140px;
}

.fha-filter-bar input:focus,
.fha-filter-bar select:focus {
  outline: none;
  border-color: var(--fha-primary);
  box-shadow: var(--fha-shadow);
}

.fha-link {
  color: var(--fha-primary);
  text-decoration: none;
}

.fha-link:hover {
  text-decoration: underline;
}

.fha-alert {
  border-radius: 10px;
  padding: 12px 14px;
  margin-bottom: 12px;
  border: 1px solid transparent;
}
.fha-alert-danger { background: var(--fha-danger); color: white; border-color: var(--fha-danger); }
.fha-alert-info { background: var(--fha-info); color: white; border-color: var(--fha-info); }

@media (max-width: 768px) {
  .fha-stat-card {
    flex: 1 1 45%;
  }
}

@media (max-width: 480px) {
  .fha-stat-card {
    flex: 1 1 100%;
  }
}

/* ============================================
   WIDGET: FHA Documentation
   ============================================ */
/* ===== CONTAINER ===== */
.fha-page-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
  color: var(--fha-gray-800);
  line-height: 1.6;
}

/* ===== HEADER ===== */
.fha-page-header {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  padding: 48px 40px;
  margin-bottom: 0;
  box-shadow: var(--fha-shadow);
}

.fha-page-header-content {
  max-width: 1200px;
  margin: 0 auto;
}

.fha-page-header h1 {
  margin: 0 0 12px 0;
  font-size: 36px;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 16px;
}

.fha-page-header h1 i {
  font-size: 32px;
}

.fha-page-subtitle {
  font-size: 18px;
  opacity: 0.95;
  margin: 0 0 20px 0;
  font-weight: 400;
}

.fha-page-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-bottom: 20px;
  font-size: 13px;
}

.fha-page-meta-badge {
  background: var(--fha-primary-dark);
  padding: 6px 14px;
  border-radius: var(--fha-radius-lg);
  font-weight: 600;
}

.fha-page-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.fha-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border: none;
  border-radius: var(--fha-radius-lg);
  font-weight: 600;
  cursor: pointer;
  transition: var(--fha-transition);
  font-size: 14px;
  text-decoration: none;
}

.fha-btn-primary {
  background: linear-gradient(135deg, var(--fha-primary) 0%, var(--fha-primary-dark) 100%);
  color: white;
  box-shadow: var(--fha-shadow);
}

.fha-btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: var(--fha-shadow-lg);
}

.fha-btn-secondary {
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
  border: 1px solid var(--fha-gray-200);
}

.fha-btn-secondary:hover {
  background: var(--fha-gray-200);
}

/* ===== NAVIGATION ===== */
.fha-page-nav {
  background: white;
  border-bottom: 1px solid var(--fha-gray-200);
  position: sticky;
  top: 0;
  z-index: 1000;
  box-shadow: var(--fha-shadow);
}

.fha-page-nav-content {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 40px;
}

.fha-page-nav-scroll {
  overflow-x: auto;
  scrollbar-width: thin;
}

.fha-page-nav-scroll::-webkit-scrollbar {
  height: 4px;
}

.fha-page-nav-scroll::-webkit-scrollbar-track {
  background: var(--fha-gray-100);
}

.fha-page-nav-scroll::-webkit-scrollbar-thumb {
  background: var(--fha-gray-200);
  border-radius: var(--fha-radius);
}

.fha-page-nav-items {
  display: flex;
  gap: 4px;
  padding: 12px 0;
  min-width: max-content;
}

.fha-page-nav-btn {
  padding: 8px 16px;
  border: none;
  background: transparent;
  color: var(--fha-gray-500);
  border-radius: var(--fha-radius);
  cursor: pointer;
  font-size: 14px;
  font-weight: 500;
  transition: var(--fha-transition);
  white-space: nowrap;
  display: flex;
  align-items: center;
  gap: 6px;
}

.fha-page-nav-btn:hover {
  background: var(--fha-gray-100);
  color: var(--fha-gray-800);
}

.fha-page-nav-btn.active {
  background: var(--fha-primary);
  color: white;
  font-weight: 600;
}

.fha-page-nav-btn i {
  font-size: 13px;
}

/* ===== CONTENT ===== */
.fha-page-content {
  max-width: 1200px;
  margin: 0 auto;
  padding: 40px 40px 80px;
}

.fha-page-section {
  background: white;
  border-radius: var(--fha-radius-lg);
  padding: 40px;
  margin-bottom: 32px;
  box-shadow: var(--fha-shadow);
  border: 1px solid var(--fha-gray-200);
}

.fha-page-section h2 {
  color: var(--fha-primary);
  font-size: 28px;
  margin: 0 0 24px 0;
  padding-bottom: 16px;
  border-bottom: 2px solid var(--fha-primary-light);
  display: flex;
  align-items: center;
  gap: 12px;
  font-weight: 700;
}

.fha-page-section h2 i {
  font-size: 24px;
}

.fha-page-section h3 {
  color: var(--fha-gray-800);
  font-size: 20px;
  margin: 32px 0 16px 0;
  font-weight: 600;
}

.fha-page-section h4 {
  color: var(--fha-gray-800);
  font-size: 16px;
  margin: 24px 0 12px 0;
  font-weight: 600;
}

.fha-page-section p {
  color: var(--fha-gray-500);
  line-height: 1.7;
  margin-bottom: 16px;
  font-size: 15px;
}

.fha-page-section ul,
.fha-page-section ol {
  margin: 16px 0;
  padding-left: 28px;
  color: var(--fha-gray-500);
}

.fha-page-section li {
  margin-bottom: 8px;
  line-height: 1.6;
}

/* ===== TABLES ===== */
.fha-page-table {
  width: 100%;
  border-collapse: collapse;
  margin: 24px 0;
  font-size: 14px;
  border-radius: var(--fha-radius);
  overflow: hidden;
  box-shadow: var(--fha-shadow);
}

.fha-page-table thead {
  background: var(--fha-gray-100);
}

.fha-page-table th {
  padding: 14px 16px;
  text-align: left;
  font-weight: 600;
  color: var(--fha-gray-800);
  border-bottom: 2px solid var(--fha-gray-200);
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.fha-page-table td {
  padding: 14px 16px;
  border-bottom: 1px solid var(--fha-gray-200);
  vertical-align: top;
}

.fha-page-table tbody tr:hover {
  background: var(--fha-gray-100);
}

.fha-page-table tbody tr:last-child td {
  border-bottom: none;
}

/* ===== BADGES & LABELS ===== */
.fha-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 4px 10px;
  border-radius: var(--fha-radius);
  font-size: 12px;
  font-weight: 600;
  white-space: nowrap;
}

.fha-badge-success {
  background: var(--fha-success);
  color: white;
}

.fha-badge-warning {
  background: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-badge-danger {
  background: var(--fha-danger);
  color: white;
}

.fha-badge-info {
  background: var(--fha-primary);
  color: white;
}

/* ===== CODE ===== */
.fha-code-inline {
  background: var(--fha-gray-100);
  color: var(--fha-primary);
  padding: 2px 8px;
  border-radius: var(--fha-radius);
  font-family: 'Monaco', 'Menlo', 'Courier New', monospace;
  font-size: 13px;
  font-weight: 500;
}

.fha-code-block {
  background: var(--fha-gray-800);
  color: var(--fha-gray-100);
  padding: 20px;
  border-radius: var(--fha-radius);
  font-family: 'Monaco', 'Menlo', 'Courier New', monospace;
  font-size: 13px;
  overflow-x: auto;
  margin: 16px 0;
  line-height: 1.6;
  box-shadow: var(--fha-shadow);
}

/* ===== INFO BOXES ===== */
.fha-info-box {
  background: var(--fha-primary-light);
  border-left: 4px solid var(--fha-primary);
  padding: 16px 20px;
  border-radius: 0 var(--fha-radius) var(--fha-radius) 0;
  margin: 20px 0;
  color: var(--fha-gray-800);
}

.fha-info-box.success {
  background: var(--fha-success);
  border-left-color: var(--fha-success);
  color: white;
}

.fha-info-box.warning {
  background: var(--fha-warning);
  border-left-color: var(--fha-warning);
  color: var(--fha-gray-800);
}

.fha-info-box.danger {
  background: var(--fha-danger);
  border-left-color: var(--fha-danger);
  color: white;
}

.fha-info-box p:last-child {
  margin-bottom: 0;
}

/* ===== CARDS ===== */
.fha-card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin: 24px 0;
}

.fha-card {
  background: white;
  border: 1px solid var(--fha-gray-200);
  border-radius: var(--fha-radius-lg);
  padding: 20px;
  transition: var(--fha-transition);
}

.fha-card:hover {
  box-shadow: var(--fha-shadow-lg);
  transform: translateY(-2px);
}

.fha-card h4 {
  color: var(--fha-primary);
  margin: 0 0 12px 0;
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 16px;
}

.fha-card p {
  margin: 0 0 12px 0;
  font-size: 14px;
}

.fha-card ul {
  margin: 0;
  padding-left: 20px;
  font-size: 14px;
}

/* ===== STATS ===== */
.fha-stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin: 24px 0;
}

.fha-stat-card {
  background: white;
  border: 2px solid var(--fha-gray-200);
  border-radius: var(--fha-radius);
  padding: 20px;
  text-align: center;
  transition: var(--fha-transition);
}

.fha-stat-card:hover {
  border-color: var(--fha-primary);
  box-shadow: var(--fha-shadow-lg);
}

.fha-stat-number {
  font-size: 32px;
  font-weight: 700;
  color: var(--fha-primary);
  margin: 0 0 8px 0;
}

.fha-stat-label {
  font-size: 13px;
  color: var(--fha-gray-500);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 768px) {
  .fha-page-header {
    padding: 32px 24px;
  }
  
  .fha-page-header h1 {
    font-size: 28px;
  }
  
  .fha-page-nav-content {
    padding: 0 24px;
  }
  
  .fha-page-content {
    padding: 24px 24px 60px;
  }
  
  .fha-page-section {
    padding: 24px;
  }
  
  .fha-page-section h2 {
    font-size: 24px;
  }
  
  .fha-card-grid,
  .fha-stats-grid {
    grid-template-columns: 1fr;
  }
  
  .fha-page-table {
    font-size: 12px;
  }
  
  .fha-page-table th,
  .fha-page-table td {
    padding: 10px 12px;
  }
}

/* ===== UTILITIES ===== */
.fha-divider {
  height: 1px;
  background: var(--fha-gray-200);
  margin: 24px 0;
}

.fha-text-center {
  text-align: center;
}

/* ===== ANIMATIONS ===== */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fha-page-section {
  animation: fadeIn 0.4s ease-out;
}</css_variable>
        <footer/>
        <footer_fixed>true</footer_fixed>
        <header display_value="FHA Header">b130c79483deb21083e1b4a6feaad37a</header>
        <matching_now_experience_theme/>
        <name>FHA Theme</name>
        <navbar_fixed>true</navbar_fixed>
        <sys_class_name>sp_theme</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-01-17 20:25:23</sys_created_on>
        <sys_id>f6907e3053ae7e10c7233ee0a0490e38</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_name>FHA Theme</sys_name>
        <sys_package display_value="Foundation Health Analyzer" source="x_1310794_founda_0">d852994c8312321083e1b4a6feaad3e6</sys_package>
        <sys_policy/>
        <sys_scope display_value="Foundation Health Analyzer">d852994c8312321083e1b4a6feaad3e6</sys_scope>
        <sys_update_name>sp_theme_f6907e3053ae7e10c7233ee0a0490e38</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-01-17 20:25:23</sys_updated_on>
        <turn_off_scss_compilation>false</turn_off_scss_compilation>
    </sp_theme>
</record_update>
